<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        <!-- Module Category -->
        <record id="module_category_asset_management" model="ir.module.category">
            <field name="name">Asset Management</field>
            <field name="description">Helps you manage your company assets.</field>
            <field name="sequence">10</field>
        </record>

        <!-- User Group -->
        <record id="group_asset_user" model="res.groups">
            <field name="name">User</field>
            <field name="category_id" ref="module_category_asset_management"/>
            <field name="implied_ids" eval="[(4, ref('base.group_user'))]"/>
            <field name="comment">The user will be able to view assets and categories.</field>
        </record>

        <!-- Manager Group -->
        <record id="group_asset_manager" model="res.groups">
            <field name="name">Manager</field>
            <field name="category_id" ref="module_category_asset_management"/>
            <field name="implied_ids" eval="[(4, ref('group_asset_user'))]"/>
            <field name="users" eval="[(4, ref('base.user_root')), (4, ref('base.user_admin'))]"/>
            <field name="comment">The manager will be able to create, edit and delete assets and categories.</field>
        </record>
    </data>
</odoo>
